---
{{- if .Values.ikg.enabled }}
{{- if .Values.ikg.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ tpl .Values.ikg.fullName $ }}
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod-traefik
    kubernetes.io/ingress.class: traefik
    traefik.ingress.kubernetes.io/router.middlewares: traefik-ip-allow-list-with-office@kubernetescrd
spec:
  rules:
    - host: {{ .Values.ikg.ingress.hostname }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ .Values.ikg.fullName }}
                port:
                  name: http
  tls:
    - hosts:
        - {{ .Values.ikg.ingress.hostname }}
      secretName: {{ printf "%s-tls" .Values.ikg.ingress.hostname }}
{{- end }}
{{- end }}
